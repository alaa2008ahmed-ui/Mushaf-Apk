# قواطع وأحكام أساسية للبيانات (CRITICAL DATA INTEGRITY MANDATE)

1. **حماية البيانات وحظر التعديلات إلا بكلمة السر (Strict Password Authorization for Employee Modifications)**:
   - يُمنع منعاً باتاً تعديل أو تغيير أو مسح أو إضافة أي بيانات خاصة بالموظفين (الأسماء، الوظائف، الراتب الأساسي، البدلات، التواريخ، أو أي حقل آخر بالكامل) سواء في واجهة المستخدم أو قاعدة البيانات إلا بعد طلب إدخال كلمة السر المصرح بها أولاً وهي: `0120301012`.
   - تم تصميم واجهة النظام لتطلب كلمة السر `0120301012` عند محاولة الإضافة أو الحذف أو التعديل على أي موظف.

2. **حماية السجلات المرجعية الأساسية**:
   - لا يتم التعديل على البيانات المرجعية الأساسية للموظفين (الملف المرجعي `src/data.ts`) إلا بتوجيه صريح ومباشر من المستخدم ومطابق للمستندات والصور المرفقة الرسمية.

3. **دقة المزامنة والتوافق**:
   - يتم الحفاظ الكامل على توافق الحسابات والمزامنة التلقائية مع قاعدة البيانات وحساب المخصصات بدقة تامة.
