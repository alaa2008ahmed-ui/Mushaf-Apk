import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.personalexpenses.app',
  appName: 'personal expenses',
  webDir: 'dist'
};

export default config;
